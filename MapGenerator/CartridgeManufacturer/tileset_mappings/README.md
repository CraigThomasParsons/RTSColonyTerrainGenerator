# Tileset Mappings

Map WCAR terrain names to StarCraft MTXM tile indices.

Each mapping file defines:
- `tileset`: one of badlands, space_platform, installation, ashworld, jungle, desert, ice, twilight
- `default_tile`: fallback MTXM index
- `terrain_to_tile`: per-terrain overrides
