# HeightmapApi

API service for managing heightmap generation requests.

## Directory Structure

- `systemd/` - Systemd service configuration files
- `inbox/` - Incoming requests for heightmap generation
- `outbox/` - Completed heightmap API responses
- `archive/` - Archived processed requests
