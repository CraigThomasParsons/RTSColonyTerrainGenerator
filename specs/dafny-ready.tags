# Promotion ledger — verified rules (mirrors tests/bdd/net-ready.tags for the formal side).
# One @slice-NN tag per line. A slice is listed only when its correctness-critical rules
# have a verifying Dafny model (Level A) or verified reference implementation (Level B)
# under specs/, with a verification report in the slice's spec pack. Appending a tag is an
# explicit, reviewable act in the slice's closing PR; weakening a spec to get here requires
# human approval (AGENTS.md, "Specification Protection").
# Lines starting with '#' are comments.
@slice-01-cell-to-tile
@slice-02-adjacency-mask
