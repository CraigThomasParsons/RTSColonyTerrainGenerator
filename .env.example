###############################################################################
# MapGenerator – Global Environment Configuration
#
# This file controls HUMAN-ONLY debug behavior and safe runtime toggles.
#
# Inbox / Outbox / Archive directories MUST remain machine-only.
# Debug output is written to a separate directory explicitly for inspection.
###############################################################################

# ---------------------------------------------------------------------------
# Global debug toggle (future use)
# ---------------------------------------------------------------------------
MAPGEN_DEBUG_HEIGHTMAP_BMP=1
MAPGEN_DEBUG_LAYER_BMP=1
TILER_DEBUG_HTML=1
TREEPLANTER_DEBUG_HTML=1

# ---------------------------------------------------------------------------
# Debug output location
#
# This directory is used ONLY for human inspection artifacts
# (BMPs, logs, visualizations, etc.)
#
# It MUST NOT be an inbox, outbox, or archive directory.
# ---------------------------------------------------------------------------
MAPGEN_DEBUG_OUTPUT_DIR=/home/youruser/Code/RTSColonyTerrainGenerator/MapGenerator/Heightmap/debug

# ---------------------------------------------------------------------------
# Heightmap module debug flags
# ---------------------------------------------------------------------------

# Write grayscale height BMPs (0–255 height visualization)
MAPGEN_DEBUG_HEIGHTMAP_BMP=1

# Write terrain layer BMPs (water / land / mountains)
MAPGEN_DEBUG_LAYER_BMP=1

# Heightmap generation tuning
# Number of fault-line iterations to run
# Higher = more rugged terrain
HEIGHTMAP_FAULT_ITERATIONS=50

# The amount of snoothing: box blur / neighborhood averageing. 2 = 2 passes
HEIGHTMAP_SMOOTHING_PASSES=2

###############################################################################
# Heightmap generation tuning
# Number of fault-line iterations to run
# Higher = more rugged terrain
# HEIGHTMAP_FAULT_ITERATIONS=240
# Might never do heightmap tuning again.
# If I try to do it again think about this: You wanted the iterations to scale
# to the size of the maps.
################################################################################
# The amount of snoothing: box blur / neighborhood averageing. 2 = 2 passes
#HEIGHTMAP_SMOOTHING_PASSES=12

# ---------------------------------------------------------------------------
# mapgenctl TUI configuration
# ---------------------------------------------------------------------------

# Number of log lines to display in the TUI ring buffer.
# Increase to see logs from more concurrent pipeline stages.
MAPGENCTL_LOG_BUFFER_SIZE=8